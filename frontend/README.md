# AnimeBox frontend

React + TypeScript + Tailwind UI for the local AnimeBox server.

```bash
npm install
npm run dev      # http://127.0.0.1:5173 (proxies /api → :8787)
npm run build    # outputs to dist/ (served by FastAPI)
```
